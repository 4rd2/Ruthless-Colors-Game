{
    "name": "ruthless-colors",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
        "dev:server": "cd server && npm run dev",
        "dev:client": "cd client && npm run dev",
        "build": "npm install --prefix shared && npm install --prefix server && npm install --prefix client && cd client && npm run build",
        "start": "cd server && npm start",
        "install:all": "npm install --prefix shared && npm install --prefix server && npm install --prefix client"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "dependencies": {
        "@tailwindcss/vite": "^4.2.2",
        "@vercel/analytics": "^2.0.1",
        "tailwindcss": "^4.2.2"
    }
}
